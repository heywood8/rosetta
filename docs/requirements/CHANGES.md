# Requirements Change Log

- 2026-02-15: Initial draft of Rosetta CLI requirements, `configure` command (FR-0001 through FR-0016, NFR-0001 through NFR-0005).
- 2026-02-15: Applied decisions: MCP server name "Rosetta", native CLI tools for claudecode/codex/opencode, windsurf kept as regular draft target, bootstrap bundled from single source of truth.
- 2026-02-15: Added workspace and rosetta.json requirements (FR-0017 through FR-0022): workspace root = CWD, rosetta.json for VCS-safe persistence, no secrets in file, re-use on subsequent runs.
- 2026-02-15: Normalized Windsurf handling in requirements: treated as a regular target, removed duplicate deferred notes, kept single draft-status note in FR-0010.
- 2026-02-16: Renamed "profile" to "MCP preset" (stdio, http). Added "application profile" concept (FR-0023). Extracted FR-0004–FR-0007 to mcp-presets.md, FR-0023 to application-profiles.md. Refactored FR-0010, FR-0012 to reference application profiles. Updated glossary.
- 2026-04-22: rosettify — envelope is now internal. Updated FR-ARCH-0011 to state that the EnrichedEnvelope is never directly serialized for consumers. Added FR-ARCH-0014 (frontend output transformation): frontends extract result on success, sanitized error+help on failure — the envelope wrapper is never the output. Updated FR-CLI-0004 and FR-MCP-0003 accordingly. Added FR-SHRD-0007 (frontend failure logging): all failures logged via shared logger (ERROR for internal_error, WARN for all others) before output is written.
- 2026-05-17: rosettify — added FR-PKG-0005 (Semantic Versioning). package.json `version` SHALL follow semver 2.0.0; MAJOR for any breaking change to CLI/MCP/plan-schema/envelope/error-code contracts, MINOR for backwards-compatible additions (new subcommand, new optional field, new template), PATCH for non-contract bug fixes. Every behavior-changing release SHALL be accompanied by a release-notes entry. Motivation: prevent contract loopholes like the prior FR-PLAN-0024 rename→mutex rewrite from shipping as a silent patch.
- 2026-05-15: rosettify — FR-PLAN-0024 rewritten to mandate a cross-process mutex (atomic-create primitive: mkdir of a lock directory, or O_CREAT|O_EXCL file marker) as the cycle's exclusion gate, and to explicitly forbid plain rename or plain hardlink as the exclusion primitive. Renamed "rename-as-guard" wording to "atomic mutex" throughout. Added a "Concurrency primitive constraints (lessons learned)" block documenting why rename and hardlink are insufficient (verified by multi-process tests: rename lost ~10% of writes at N=10; hardlink lost ~10% at N=30). Acceptance criteria expanded to require an N≥10 concurrent-process test as evidence. The original rename-only formulation was a contract loophole that the first two implementations followed and the unit-test suite could not catch.
- 2026-05-15: rosettify — clarified FR-SHRD-0006 and FR-PLAN-0025: plan-file writes are governed exclusively by FR-PLAN-0024 (atomic-mutex cycle); FR-SHRD-0006 remains the general optimistic-concurrency utility for non-plan JSON files. Aligns intended supersession with explicit requirement text and resolves the prior ambiguity that left both mechanisms claimed for plan writes.
- 2026-05-15: rosettify — plan command extended with templates, atomic write with rename-as-guard, backup chain, and dense JSON output. Modified FR-ARCH-0007 (all output is JSON, no exemptions), FR-HELP-0002 (flat `schemas` dict + `notes` per command), FR-PLAN-0010 / FR-PLAN-0015 (result conforms to compressed-tree shape), FR-PLAN-0016 (restructured help with notes, dual-form examples, schemas dict, templates catalog), FR-PLAN-0017 (added `previous_version` field), FR-PLAN-0018 (dual-form examples mandate), FR-PLAN-0021 (new error codes: invalid_template, missing_template_param, unexpected_template_param, backup_create_failed, plan_file_corrupted), FR-PLAN-0023 (subcommand list updated). Added FR-PLAN-0024 (rename-as-guard atomic write, retention 5, 50-retry cycle restarting from step 1), FR-PLAN-0026 (plan file pretty-formatted on disk), FR-PLAN-0030 (create-with-template), FR-PLAN-0031 (upsert-with-template), FR-PLAN-0032 (list-templates), FR-PLAN-0033 (template registry: two compiled-in TS dicts), FR-PLAN-0034 (placeholder substitution with strict bidirectional matching; canonical kebab-case names span CLI/MCP/template), FR-PLAN-0035 (seed `for-orchestrator` create template), FR-PLAN-0036 (seed `for-subagent` upsert template), FR-PLAN-0040 (compressed-tree output shape for write subcommands), FR-SHRD-0008 (dense JSON output payloads, plan file on disk excepted), FR-SHRD-0009 (read resilience: 100 ms × 50 retries). Asset files added under `docs/requirements/rosettify/assets/templates/`: create-for-orchestrator.json, upsert-for-subagent.json.
- 2026-05-29: rosettify — plan help-clarity follow-up (pass 2). Tightened FR-HELP-0002 to RECURSIVE named types: every array `items` and every nested object property exposed in `schemas` must itself be a named exported type at any depth (no anonymous shapes). Introduced named types PlanNextStep (next array item), PlanPhaseContext (next `parent`), PlanSummary (PlanWriteResult `plan`), PlanStatusTotals / PlanPhaseSummary / PlanStepSummary / PlanStepDetail (show_status, with PlanPhaseSummary/PlanStepSummary shared into PlanWriteResult), and PlanTemplateCatalogEntry. FR-PLAN-0040: removed `previous_version` from PlanWriteResult (it was an always-equal duplicate of the plan document's own field, which is retained as the recovery chain per FR-PLAN-0017/0024); FR-PLAN-0010 result-acceptance aligned. FR-PLAN-0041: schemas dict must enumerate the new named types and pass a walk/no-anonymous check. FR-PLAN-0013: named the status-summary/step-detail shapes and fixed progress_pct denominator = all in-scope steps incl. blocked/failed. FR-PLAN-0032/0035/0036: added a one-line `produces` summary per template entry. FR-PLAN-0042 notes: consolidated the end-to-end 5-step usage flow, the three terminal outcomes of a next call, recovery by re-review/re-verify/status-reset, and `--target` usable with or without a limit. FR-PLAN-0016: per-subcommand (conditional) required-ness — upsert `kind` required only for a new target, `phase_id` only when `kind=step`; status-propagation precedence stated; next_steps_for_ai rewritten to the three outcomes. Dropped from scope: per-subcommand error-code listing (errors stay self-explaining) and backup-name note (path visible via previous_version). Independent reviewer pass applied (PASS-WITH-FIXES; all 6 findings resolved). Source backlog: plans/rosettify/plan-help-improvements-proposal.md (P1–P10).
- 2026-05-29: rosettify — plan `next` output redesign, help clarity, and no-internal-leak. Added FR-ARCH-0016 (emitted output — results, errors, help, schema descriptions, notes — must contain no requirement IDs, ticket IDs, internal source paths, internal module names, or dataset prefixes; emitted notes are standalone directive guidance with no authoring rationale). Updated FR-HELP-0002 (help `schemas` dictionary keyed by exported type name; every exposed shape is a named exported type, no anonymous shapes; SRP+DRY — shapes identical in structure and purpose defined once and reused, one entry per distinct type). FR-PLAN-0011 (next): result is `PlanNextResult` = {parent (present only with target_id; targeted phase scalar fields, no steps collection), next[] (in_progress→open→blocked→failed, truncated to limit), count, plan_status, OverallOpenCount/OverallInProgressCount/OverallBlockedCount/OverallFailedCount/OverallCompleteCount (scoped to the filtered phase, else whole plan)}; default limit 3 (overridable); each returned step carries its own `status` and no redundant per-step flags; blocked/failed/in_progress retrieved at any scale via show_status → query → update_status. FR-PLAN-0040: write-result shape named `PlanWriteResult`, a single shared type for all four write subcommands. FR-PLAN-0012/0013/0014: result types named PlanUpdateStatusResult / PlanShowStatusResult / PlanQueryResult. FR-PLAN-0010/0015: reference PlanWriteResult. Split FR-PLAN-0016 (help-content manifest) — extracted FR-PLAN-0041 (schemas-dictionary content) and FR-PLAN-0042 (notes content; adds plan-construction-flow, phase-scoped execution via `next --target`, and the next/show_status/query/update_status recovery guidance). Independent reviewer pass applied (PASS-WITH-FIXES).
- 2026-05-31: rosettify — surfaced the backup link in write outputs for self-discoverability (reverses the earlier drop). FR-PLAN-0040: PlanWriteResult's plan summary now carries `previous_version` (PlanSummary = {name, status, previous_version}) — null on a first create, the just-created backup path otherwise, equal to the plan document's value; the plan document remains the source of truth and recovery chain. Rationale flipped from DRY (the field was a duplicate that no code path read) to self-discovery (an agent acts on outputs, not docs, so the backup link belongs in the write result). FR-PLAN-0010 result-acceptance aligned (result.plan.previous_version=null on first create).
- 2026-05-31: rosettify — released 2.3.0 (MINOR — backwards-compatible additions only). FR-PLAN-0011: the CLI `next` limit now also accepts a hidden `--limit <n>` compatibility alias (undocumented; never shown in help/schemas/output; positional wins on conflict), and the CLI default limit was corrected from a hardcoded 10 to the documented 3. Added FR-PKG-0006: the version reported by the CLI (`--version`/`version`), the MCP server, and the help payload SHALL be read from package.json (single source of truth) — fixing a drift where all three hardcoded 0.1.0 while package.json advanced.
- 2026-06-02: rosettify — added `phase-steps` required parameter to create-with-template and upsert-with-template. FR-PLAN-0030: updated to five required inputs, added phase-steps table row and not-a-placeholder clarification, extended acceptance criteria. FR-PLAN-0031: updated to six required inputs, added phase-steps table row and not-a-placeholder clarification, extended acceptance criteria. FR-PLAN-0034: appended clarification that phase-steps injection is governed by FR-PLAN-0043 and is outside placeholder substitution. FR-PLAN-0016 and FR-PLAN-0018: updated create-with-template dual-form examples to include trailing phase-steps argument. FR-PLAN-0021: added `invalid_phase_steps` error code, updated `missing_template_param` description to cover phase-steps. Added FR-PLAN-0043 (Draft): phase-steps array injection — required JSON string input parsed to step array, appended after template render, validated downstream; errors: missing_template_param: phase-steps and invalid_phase_steps. INDEX.md: rewritten with greppable per-file `##`/`###` headers for all rosettify requirement files and the rosetta-cli component. FR-PLAN-0042: end-to-end usage note rewritten to a build-then-execute model — create-with-template for the initial preparation phase and upsert-with-template for every follow-up phase (each passing the actual phase-steps as the main body of work), plain upsert reserved for follow-up steps and patching (rarely), step granularity ~3-5 minutes of an AI coding agent's work, whole execution plan built before execution. Released rosettify 2.4.0 (MINOR — backwards-compatible). phase-steps is recommended/required in usage and help, but for backward compatibility an omitted value is treated as an empty array rather than rejected (same recommend-in-guidance / recover-in-implementation pattern as the hidden --limit alias). FR-PLAN-0043 statement + acceptance updated (omitted → treated as []); FR-PLAN-0030/0031 acceptance updated (omitted → seeded steps only); FR-PLAN-0021 missing_template_param no longer references phase-steps absence. Help notes, usage strings, examples, and the "required inputs" framing left unchanged by design.
- 2026-06-02: rosettify — added FR-PLAN-0044 (Draft): the JSON-bearing arguments (plan data of create, patch of upsert, phase-steps of the with-template subcommands) are inline JSON strings passed directly as the command argument; help presents them with json-string hints (`plan-json-string`, `patch-json-string`, `phase-steps-json-string`) in usage and tip examples plus an inline-JSON-string note. Motivated by callers supplying a path/filename where the JSON value was expected. Updated help-content.ts (usage/args/examples + note) and FR-PLAN-0016/0018 illustration tokens accordingly.
- 2026-06-04: plugin-generator — initial Draft component reverse-engineered from `scripts/plugin_generator.py`. Added MODEL.md (DATA-CFG-0001..0004), FR-CLI.md (FR-CLI-0001..0042), FR-COPY.md (FR-COPY-0001..0034), FR-GEN.md (FR-GEN-0001..0011), FR-HOOK.md (FR-HOOK-0001..0022), FR-VAR.md (FR-VAR-0010..0060), NFR.md (NFR-0001..0009), plus SCOPE/GLOSSARY/ASSUMPTIONS. New user-requested capabilities: FR-CLI-0030 (`--domain` parameter, default `core`, replacing the hardcoded source layer) and FR-CLI-0031 (comma-separated multi-domain bundling — concatenate same-path documents in domain order, override deferred to future). Captured intent corrections: FR-CLI-0040 (uniform per-target generation — the two-pass main→standalone derivation is an implementation accident, ASSUMPTIONS AC-1/QF-4, not a requirement). Companion analysis at `docs/plugin-generator/analysis.md`. All units Draft pending user approval.
- 2026-06-04: plugin-generator — added FR-ARCH.md (FR-ARCH-0001..0050): user-authored target architecture for the TS/npx rewrite. Uniform spec contract with values in `plugin-specs.ts`; immutable flat VFS (`{path, files:[{origin, frontmatter, order, conditions}]}`) built from structure + filename directives only; filename directive grammar `name.[order,<target>-only,overwrite].ext` (order-first-optional, known-tokens-once, unknown=error, bracket stripped for output); pure processor pipeline (frame `{path, target, isBinary, target_contents, source}`, plugin→file→processor order, content I/O confined to read/write); processor contracts read()/apply_overrides()/bundle()/rename()/codex_agent_format()/write() with dry-run in write(); decision+I/O logging excluding content. Added FR-CLI-0050 (--dry-run), FR-CLI-0051 (--verbose); NFR-0010 (lightweight libs pinned to rosettify: commander ^14, pino ^10, handlebars; ESM/Node≥22/TS6/vitest). Resolved ASSUMPTIONS OQ-1 (bundle = plain concat, no XML) and OQ-2 (override now via directive); added AC-7/8/9 and OQ-5. FR-ARCH supersedes as-is FR-COPY/GEN/HOOK mechanisms for the rewrite while as-is FRs remain the parity reference.
- 2026-06-04: plugin-generator — FR-ARCH refinements. Added FR-ARCH-0034 (general processor input validation / fail-fast; bundle()'s binary-with-multiple-entries error recast as one example). Added FR-ARCH-0036 (`target_contents` tri-state: `null`=removed/no file written, empty=file with optional frontmatter and empty body, content=file written); FR-ARCH-0045 write() updated to honor the states. Adopted precise rewrite terminology `VirtualFile` (VFS entry) and `SourceFile` (contributing file) across FR-ARCH-0010/0012/0024/0030/0032/0040/0041/0042 and GLOSSARY (added VirtualFile, SourceFile, target_contents). Added FR-CLI-0060 (comprehensive help: commands/args PLUS origin source structure, override+bundling behavior, processors, and plugin specs).
- 2026-06-04: plugin-generator — precise terminology for EVERY concept (not only files). Added FR-ARCH-0003 (precise specific named type per concept; forbid bare generic words). Established and applied canonical types across FR-ARCH + GLOSSARY: PluginTarget, PluginSpec, SpecEntry, Processor, ProcessorPipeline, ProcessingFrame (distinct from the immutable VirtualFile), FilenameDirective, DirectiveToken (OrderToken/TargetOnlyToken/OverwriteToken), SourceFileConditions, ModelVocabulary. Renamed requirement titles/statements/acceptance accordingly (FR-ARCH-0002/0010/0012/0020/0021/0022/0023/0024/0030/0031/0032/0034/0040/0041/0042/0050; FR-CLI-0051).
- 2026-06-04: plugin-generator — added FR-ARCH-0046/0047/0048 (normalize_models, generate_index, render_template processors — closing the gap where model normalization, index generation, and template rendering had no processor in the pipeline); glossary Processor list updated. NFR-0004 + ASSUMPTIONS QF-2 recast the oversize-bootstrap behavior as an intended soft error (report, still emit, non-zero exit). Added REFERENCES.md (INT-IDE-0001..0003): any agent working on or consuming these requirements must consult the authoritative per-IDE guides under instructions/r3/core/configure/ (claude-code, cursor, github-copilot, codex, antigravity, jetbrains-junie, opencode, windsurf) for plugin/subagent/skill/command/rule/hook structure, bootstrap-delivery capability, and links; INDEX updated.
- 2026-06-04: plugin-generator — renamed FR-VAR.md → FR-PLUGINS.md and expanded it with per-target structure + reasoning and the bootstrap-delivery dimension. Added FR-VAR-0070 (each target declares one bootstrap-delivery strategy: session-start hooks | native rules | auto-loaded instructions; native-loading targets carry no session-start bootstrap hook), FR-VAR-0071 (targets with a separate in-repo distribution provide both marketplace- and standalone-form hook templates), FR-VAR-0072 (standalones inject the workflow index + plugin-root instructions into the auto-loaded rule/instruction file — reason documented, not mechanism). Existing FR-VAR-0010..0060 migrated unchanged (IDs stable). INDEX updated.
- 2026-06-04: plugin-generator — (a) ALL 101 requirement units set to `<implementation>NotStarted</implementation>` with empty implementationNotes: requirements are reverse-engineered from the Python generator but target a TypeScript/npx rewrite, so nothing is implemented in the target yet (Python is irrelevant to implementation status; source grounding stays only in analysis.md). (b) Added STRUCTURES.md (FR-STRUCT-0010/0020): generalized example folder structure per target with preserved-vs-generated marks and concise provenance ("← from / after"), grounded in a v3 build into agents/TEMP. (c) FR-VAR-0050/0051 rewritten to state standalones are generated FROM THE INSTRUCTION SOURCE by re-applying the IDE sibling's adaptations + standalone layout, NOT by consuming the marketplace target's output (the derive-from-main coupling is an accident, AC-1). INDEX updated (STRUCTURES added; Status section rewritten).
- 2026-06-04: plugin-generator — renamed FR-PLUGINS.md back to FR-VAR.md (filename only; `FR-VAR-*` IDs and content unchanged). INDEX updated.
- 2026-06-04: plugin-generator — implementationNotes purpose corrected and rewritten by status across all files: Implemented → concise aggregated files affected (file-level, no line numbers/prose); NotStarted/Planned/ToBeRemoved → empty; ToBeModified → what was originally documented but now dropped ("Drops …"). Removed all as-is line-level/source provenance from requirements (source grounding remains only in docs/plugin-generator/analysis.md). Also: item-4 — FR-HOOK-0005, FR-ARCH-0044, and the Codex variant req now defer to the per-IDE guides (INT-IDE-0002) for exact hook schema / subagent format instead of restating shapes (Option A, no duplication). Added FR-ARCH-0046/0047/0048 (normalize_models/generate_index/render_template processors). NFR-0004 + QF-2 recast oversize-bootstrap as intended soft error. Added REFERENCES.md (INT-IDE-0001..0003). Renamed FR-VAR.md → FR-PLUGINS.md (added FR-VAR-0070/0071/0072 bootstrap-delivery + two-hook-set + injection-rationale).
- 2026-06-04: plugin-generator — schema upgrade across all 90 requirement units (MODEL, FR-CLI, FR-COPY, FR-GEN, FR-HOOK, FR-VAR, FR-ARCH, NFR). Added `ticketId=""` attribute and `<approved_by></approved_by>` element; allowed `status` value `Removed`; split `<implementation>[Status: X] notes</implementation>` into status-only `<implementation>X</implementation>` plus `<implementationNotes>notes</implementationNotes>` (X ∈ NotStarted|Implemented|Planned|ToBeModified|ToBeRemoved). Content, IDs, statements, acceptance, depends, and file:line references unchanged. Performed by parallel subagents over disjoint files; verified (per-file req count == ticketId == approved_by == implementation == implementationNotes; no `[Status:` remnants).
- 2026-06-04: plugin-generator — preserved-file source + copy-first generation. Captured a missed behavior: the generator wrote on top of committed preserved files (IDE manifests `plugin.json`, hook templates `*.tmpl`, config-folder contents, any `.mcp.json`) that had to already exist in the output tree, so it could not produce a plugin into a clean/empty output directory (discovery §7, validation report). Added DATA-CFG-0005 (committed preserved-file source at `src/plugin-generator/plugins/<target>/` mirroring the output-relative layout; grounded the per-target preserved set against the live `plugins/` tree: claude `.claude-plugin/plugin.json`+`hooks/hooks.json.tmpl`; cursor `.cursor-plugin/plugin.json`+`hooks/hooks.json.tmpl`+root `hooks.json.tmpl`; copilot `.github/plugin/plugin.json`+`.github/plugin/hooks.json.tmpl`+`hooks/hooks.json.tmpl`; codex `.codex-plugin/plugin.json`+`.codex-plugin/hooks.json.tmpl`). Added FR-SEED-0001 (seed preserved files from `src/plugin-generator/plugins/<target>/` into the output before generating instruction-derived content; clean/empty-dir reproducibility) and FR-SEED-0002 (standalone preserved files derive from the parent target's `src/plugin-generator/plugins/<parent>` set — standalone-form template + parent manifest version — not from an independent config folder). Amended FR-COPY-0001 (clarified seed→wipe relationship; preserved set first established from source on a clean run, kept across re-wipe), NFR-0003 (clean/empty-dir reproducibility), FR-VAR-0050/0051/0060 (depend on FR-SEED-0002; manifest version from parent preserved manifest). Updated SCOPE (in-scope sourcing+seeding; preserved-configuration constraint), GLOSSARY (added "Preserved file" and "Preserved-file source"), ASSUMPTIONS (AC-3 scoped to `*.js` bundles only; added AC-3a resolving the prior implicit "externally seeded manifests/templates" precondition), INDEX (MODEL + FR-COPY descriptions). No existing IDs renumbered; no Must-vs-Must contradiction found (FR-COPY-0001 reset semantics preserved). All new units Draft, NotStarted, pending user approval.
- 2026-06-04: plugin-generator — **architecture unification onto a two-tier processor model + byte-for-byte parity** (user-driven; resolves the SRP/dual-architecture complaint that FR-COPY/FR-VAR/FR-HOOK re-imported the original Python procedure `copy→preCleanup→preMoveFiles→renames→indexes→inject→manifest` alongside FR-ARCH). FR-ARCH rewritten as the single normative architecture with two pure single-responsibility tiers: **FileProcessor** over a **FileProcessingFrame** (`fileRead`/`fileApplyOverrides`/`fileBundle`/`fileNormalizeModels`/`fileRename`/`fileCodexAgentFormat`) and **PluginProcessor** over a **PluginProcessingFrame** `{spec, vfs, frames, templateContext}` (`pluginCleanup`/`pluginCopy`/`pluginProcessSpecEntries`/`pluginRewriteReferences`/`pluginGenerateIndexes`/`pluginInjectSections`/`pluginAssembleBootstrap`/`pluginRenderTemplates`/`pluginWrite`). Naming convention: PascalCase types, camelCase factories with `file`/`plugin` tier prefix (FR-ARCH-0003). Key new/changed units: FR-ARCH-0014 (immutability with structural sharing — no input mutation, copy-on-write reusing unchanged sub-objects); FR-ARCH-0039 (PluginProcessingFrame, incl. `templateContext` carrying release vars + assembled bootstrap placeholders from `pluginAssembleBootstrap`→`pluginRenderTemplates`); FR-ARCH-0035 (every step is a processor at its tier — no pre/post passes; `pluginCleanup`/`pluginCopy` replace the wipe/seed "filesystem steps"); FR-ARCH-0037 (exact matching mandate — every regex anchored/complete-token, fixing the `agents`→`.codex/agents` bare-word regression, QF-7; also fixes tag-membership substring, FR-GEN-0003); FR-ARCH-0038 (generated content built against final paths → no rewriting); FR-ARCH-0043 `fileRename` path-only; **FR-ARCH-0049 `pluginRewriteReferences`** content-only, deriving its `{sourcePath→targetPath}` lookup by reading the per-file `frames` (renames observed, never recomputed — the maintainer's DRY requirement) plus `SpecEntry` folder pairs; FR-ARCH-0051 `pluginInjectSections`; new FR-ARCH-0052/0053/0054/0055 (`pluginCleanup`/`pluginCopy`/`pluginProcessSpecEntries`/`pluginAssembleBootstrap`). **SpecEntry** restored as `{source: glob, target: path, exclude: string[], processors: FileProcessor[]}` (FR-ARCH-0002): folder move = `target` (workflows→commands), suffix = `fileRename`, omission = `exclude` (array of VFS paths; no source rename since MCP/instructions reference them — OQ-3 resolved; FR-COPY-0011 recast; FR-COPY-0033/0034 → SpecEntry/`fileRename`). Standalones generated uniform-from-source (FR-VAR-0050/0051; derive-from-main retired, AC-1/QF-4). Bootstrap-file order promoted from quirk to required deterministic manifest with designated lead (FR-HOOK-0009/0003/0001; QF-1 resolved). **NFR-0001 + AC-2 flipped to byte-for-byte parity everywhere** (recursive byte-diff oracle; generator-controlled JSON/TOML/Markdown serialization; OQ-4 subsumed). NFR-0010 extended with vetted single-pick libraries (`gray-matter` frontmatter, **`immer`** structural sharing, `fast-glob` globs, alongside commander/pino/handlebars) and new **NFR-0011** (dependency selection criteria: widely adopted + actively maintained + pinned). Resolved OQ-3/OQ-4/OQ-5 and QF-1/QF-4/QF-5/QF-6/QF-7; added AC-10/AC-11/AC-12/AC-13. Propagated the two-tier vocabulary across GLOSSARY/FR-COPY/FR-GEN/FR-HOOK/FR-VAR/MODEL/NFR/STRUCTURES/ASSUMPTIONS and both INDEX files (root + plugin-generator). `src/plugins` → `src/plugin-generator/plugins` paths reconciled. Validated: 117 units, 0 dependency cycles, 0 dangling refs, all `<implementation>NotStarted</implementation>`. All 117 units **Approved by user** (status `Approved`, `approved_by=User`) on 2026-06-04; implementation not yet started.
- 2026-06-04: plugin-generator — AC-9 resolved: filename-directive delimiters changed from `[…]` to **tilde fences + comma separators** (`name.~order,tok,tok~.ext`, e.g. `bootstrap-core-policy.~1a,claude-only,overwrite~.md`). Brackets were rejected as git-pathspec magic / glob char-class / regex char-class; `~` and `,` are valid filenames on NTFS/APFS/ext4, literal in git pathspecs, non-glob/non-regex metacharacters. Updated FR-ARCH-0020/0021 grammar and GLOSSARY (FilenameDirective) — stated positively, with the decision rationale kept only in ASSUMPTIONS AC-9 (no bracket reasoning in the normative spec text).
- 2026-06-11: plugin-generator — **Task C (Clean Architecture): identity-switch elimination** (FR-ARCH-0005 P1). All 5 violation sites eliminated. (1) `ModelVocabulary.kind` and `PluginSpec.hookEntryShape` identity-discriminant fields deleted from `types.ts` and all 6 spec definitions in `targets.ts` (FR-ARCH-0002, FR-ARCH-0004, C3, C4). (2) `fileNormalizeModels` switch dispatcher deleted; 4 shared helpers exported (`extractFrontmatterModelField`, `applyModelRewrite`, `removeModelLine`, `rewriteCodexModelFields`); 4 per-vocabulary processors created: `fileNormalizeClaudeModels`, `fileNormalizeCursorModels`, `fileNormalizeCopilotModels`, `fileNormalizeCodexModels` (FR-ARCH-0046, C1). (3) `pluginAssembleBootstrap` monolith deleted; `assembleBootstrapPayload` made callback-driven (`buildEntry`, `buildRootEntry`); 4 per-IDE assemblers created: `pluginAssembleClaudeBootstrap`, `pluginAssembleCursorBootstrap`, `pluginAssembleCopilotBootstrap`, `pluginAssembleCodexBootstrap` — each writes `templateContext['bootstrap_hooks']` (one fixed key, FR-VAR-0070, FR-ARCH-0055, C2b). (4) Full cursor bootstrap support added: `buildCursorBootstrapEntry`, `buildCursorHookPayloadJson`, `CURSOR_PLUGIN_ROOT_ENTRY` — cursor's prior `default: return null` silent-drop fixed; payload assembled but not injected (cursor template omits placeholder, FR-HOOK-0007, C2a). (5) Three `.tmpl` placeholder keys renamed from `bootstrap_hooks_<ide>` to `bootstrap_hooks` (FR-VAR-0070). Multi-vendor model ordering rationale documented in FR-ARCH-0046 `implementationNotes` and code comments: maintainers order models in frontmatter to select different providers per agent/skill. `FR-HOOK-0004` dead field `includeBootstrapRules` removed. `buildPipeline` `isStandalone` dead parameter removed. Result: tsc clean, 410 tests pass, r2/r3 parity verified. Implementation reference: `plans/plugin-generator/CLEAN-ARCHITECTURE-SPECS.md`. Status updated: FR-ARCH-0002, FR-ARCH-0004, FR-ARCH-0005, FR-ARCH-0046, FR-ARCH-0049, FR-ARCH-0055, FR-HOOK-0004, FR-HOOK-0005, FR-HOOK-0007, FR-VAR-0020, FR-VAR-0070, FR-VAR-0071, FR-VAR-0072 → Implemented.
